{{- if and .Values.stowage.enabled .Values.operator.enabled .Values.webhook.enabled .Values.webhook.selfSigned.enabled }}
{{- $cert := fromJson (include "stowage.webhookCert" .) }}
apiVersion: v1
kind: Secret
type: kubernetes.io/tls
metadata:
  name: stowage-webhook-cert
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "stowage.dashboardLabels" . | nindent 4 }}
data:
  tls.crt: {{ $cert.tlsCrt | quote }}
  tls.key: {{ $cert.tlsKey | quote }}
  ca.crt:  {{ $cert.caCrt  | quote }}
{{- end }}
