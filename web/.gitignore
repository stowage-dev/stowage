node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# SvelteKit static-adapter output. CI (and local helpers) scaffold a
# placeholder index.html when the bundle is absent, so nothing under
# /dist/ is tracked.
/dist/

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
