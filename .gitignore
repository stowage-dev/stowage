# Binaries
/bin/
/stowage
/stowage.exe

# Go build artefacts
*.test
*.out
coverage.*
private/
# SvelteKit / Node
web/node_modules/
web/.svelte-kit/
web/build/
# The full SvelteKit static bundle is built by `make frontend`. CI and
# local builds scaffold a stub index.html when the bundle is absent, so
# nothing under web/dist/ is tracked.
/web/dist/

# IDE
.idea/
.vscode/
.claude/

# OS
.DS_Store
Thumbs.db

# Local-only config / secrets
*.local.yaml
.env
.env.*
/stowage-demo.db-shm
/stowage-demo.db-wal
/stowage-demo.db
/stowage.key

# Local audit/scratch dir
/.audit/

# kind cluster kubeconfig written by scripts/e2e-bootstrap.sh
/.e2e/
